# 本地 PostgreSQL 配置
POSTGRES_DB=sku_table
POSTGRES_USER=sku_table
POSTGRES_PASSWORD=sku_table_local_password
POSTGRES_BIND_ADDRESS=127.0.0.1
POSTGRES_PORT=5432

# API 本地运行时使用；密码包含特殊字符时需先进行 URL 编码。
DATABASE_URL=postgresql://sku_table:sku_table_local_password@localhost:5432/sku_table
